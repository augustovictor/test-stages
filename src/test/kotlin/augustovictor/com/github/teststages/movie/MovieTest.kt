package augustovictor.com.github.teststages.movie

import augustovictor.com.github.teststages.UnitTest
import org.junit.Assert
import org.junit.Test
import org.junit.experimental.categories.Category

@Category(UnitTest::class)
class MovieTest {
    @Test
    fun `this is a unit test 1`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 2`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 3`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 4`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 5`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 6`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 7`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 8`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 9`() {
        Assert.assertTrue(true)
    }

    @Test
    fun `this is a unit test 10`() {
        Assert.assertTrue(true)
    }
}